# Tower Climb Public Assets

Public static CDN assets for `tower-climb-game`. This repository has no application runtime, build pipeline, database, Secrets, or persistent service, so it is intentionally `exempt_non_runtime` and does not maintain a Docker image. Asset provenance and release decisions remain in the game repository.
